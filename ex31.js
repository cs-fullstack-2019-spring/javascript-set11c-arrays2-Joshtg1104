// #### Exercise 31
// Create an array with 5 numbers.
//     Ask the user for an index.
//     Alert the user of the value of the array's index.

var count = [5, 10, 15, 20, 25];

var index =parseInt(prompt("Enter an Index"));

alert(count[index]);